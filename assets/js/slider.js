let slideIndex = 1;
showSlides(slideIndex);

// Next/previous controls
function plusSlides(n) {
  showSlides(slideIndex += n);
}

// Thumbnail image controls
function currentSlide(n) {
  showSlides(slideIndex = n);
}

function showSlides(n) {
  let i;
  let slides = document.getElementsByClassName("mySlides");
  let dots = document.getElementsByClassName("dot");
  if (n > slides.length) {slideIndex = 1}
  if (n < 1) {slideIndex = slides.length}
  for (i = 0; i < slides.length; i++) {
    slides[i].style.display = "none";
  }
  for (i = 0; i < dots.length; i++) {
    dots[i].className = dots[i].className.replace(" active", "");
  }
  slides[slideIndex-1].style.display = "block";
  // dots[slideIndex-1].className += " active";
}






let slideIndex1 = 1;
showSlides1(slideIndex1);

// Next/previous controls
function plusSlides1(n) {
  showSlides1(slideIndex1 += n);
}

// Thumbnail image controls
function currentSlide1(n) {
  showSlides1(slideIndex1 = n);
}

function showSlides1(n) {
  let i;
  let slides = document.getElementsByClassName("mySlides1");
  let dots = document.getElementsByClassName("dot1");
  if (n > slides.length) {slideIndex1 = 1}
  if (n < 1) {slideIndex1 = slides.length}
  for (i = 0; i < slides.length; i++) {
    slides[i].style.display = "none";
  }
  for (i = 0; i < dots.length; i++) {
    dots[i].className = dots[i].className.replace(" active", "");
  }
  slides[slideIndex1-1].style.display = "block";
  // dots[slideIndex1-1].className += " active";
}



let slideIndex2 = 1;
showSlides2(slideIndex2);

// Next/previous controls
function plusSlides2(n) {
  showSlides2(slideIndex2 += n);
}

// Thumbnail image controls
function currentSlide2(n) {
  showSlides2(slideIndex2 = n);
}

function showSlides2(n) {
  let i;
  let slides = document.getElementsByClassName("mySlides2");
  let dots = document.getElementsByClassName("dot2");
  if (n > slides.length) {slideIndex2 = 1}
  if (n < 1) {slideIndex2 = slides.length}
  for (i = 0; i < slides.length; i++) {
    slides[i].style.display = "none";
  }
  for (i = 0; i < dots.length; i++) {
    dots[i].className = dots[i].className.replace(" active", "");
  }
  slides[slideIndex2-1].style.display = "block";
  // dots[slideIndex1-1].className += " active";
}

let slideIndex55 = 1;
showSlides55(slideIndex55);

// Next/previous controls
function plusSlides55(n) {
  showSlides55(slideIndex55 += n);
}

// Thumbnail image controls
function currentSlide55(n) {
  showSlides55(slideIndex55 = n);
}

function showSlides55(n) {
  let i;
  let slides = document.getElementsByClassName("mySlides55");
  let dots = document.getElementsByClassName("dot55");
  if (n > slides.length) {slideIndex55 = 1}
  if (n < 1) {slideIndex55 = slides.length}
  for (i = 0; i < slides.length; i++) {
    slides[i].style.display = "none";
  }
  for (i = 0; i < dots.length; i++) {
    dots[i].className = dots[i].className.replace(" active", "");
  }
  slides[slideIndex55-1].style.display = "block";
  // dots[slideIndex-1].className += " active";
}





let slideIndex66 = 1;
showSlides66(slideIndex66);

// Next/previous controls
function plusSlides66(n) {
  showSlides66(slideIndex66 += n);
}

// Thumbnail image controls
function currentSlide66(n) {
  showSlides66(slideIndex66 = n);
}

function showSlides66(n) {
  let i;
  let slides = document.getElementsByClassName("mySlides66");
  let dots = document.getElementsByClassName("dot66");
  if (n > slides.length) {slideIndex66 = 1}
  if (n < 1) {slideIndex66 = slides.length}
  for (i = 0; i < slides.length; i++) {
    slides[i].style.display = "none";
  }
  for (i = 0; i < dots.length; i++) {
    dots[i].className = dots[i].className.replace(" active", "");
  }
  slides[slideIndex66-1].style.display = "block";
  // dots[slideIndex66-1].className += " active";
}




let slideIndex77 = 1;
showSlides77(slideIndex77);

// Next/previous controls
function plusSlides77(n) {
  showSlides77(slideIndex77 += n);
}

// Thumbnail image controls
function currentSlide77(n) {
  showSlides77(slideIndex77 = n);
}

function showSlides77(n) {
  let i;
  let slides = document.getElementsByClassName("mySlides77");
  let dots = document.getElementsByClassName("dot77");
  if (n > slides.length) {slideIndex77 = 1}
  if (n < 1) {slideIndex77 = slides.length}
  for (i = 0; i < slides.length; i++) {
    slides[i].style.display = "none";
  }
  for (i = 0; i < dots.length; i++) {
    dots[i].className = dots[i].className.replace(" active", "");
  }
  slides[slideIndex77-1].style.display = "block";
  // dots[slideIndex77-1].className += " active";
}